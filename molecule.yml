---
docker:
  containers:

      - name: ansible-role-gnome-proxy-01
        image: ubuntu
        image_version: latest
        ansible_groups:
          - group1

      - name: ansible-role-gnome-proxy-02
        image: ubuntu
        image_version: latest
        ansible_groups:
          - group1