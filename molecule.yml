---
ansible:
  playbook: tests/test.yml

driver:
  name: docker

docker:
  containers:
    - name: ansible-role-gnome-proxy-ubuntu-trusty
      image: ubuntu
      image_version: '14.04'
    - name: ansible-role-gnome-proxy-ubuntu-xenial
      image: ubuntu
      image_version: '16.04'

verifier:
  name: testinfra
