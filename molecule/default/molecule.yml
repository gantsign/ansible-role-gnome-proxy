---
dependency:
  name: galaxy

driver:
  name: docker

lint:
  name: yamllint

platforms:
  - name: ansible-role-gnome-proxy-ubuntu-trusty
    image: ubuntu:14.04
  - name: ansible-role-gnome-proxy-ubuntu-xenial
    image: ubuntu:16.04

provisioner:
  name: ansible
  lint:
    name: ansible-lint

scenario:
  name: default

verifier:
  name: testinfra
  lint:
    name: flake8
